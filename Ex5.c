#include <stdio.h>
int main (){
    int num;
    printf("Enter the number:");
    scanf("%d",&num);
    
        if(num<10); {
        printf("too low",num);
    }
        elif(10<num<20);
    {

        printf("Correct");
    }
       else(num>20);
    {
        printf("Too high");
    }
    return 0;
}
